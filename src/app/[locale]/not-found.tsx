//Test what happens if we erase this route: Woult the 404 page still appear before the locale and after locale pages?

const notFound = () => {
  return <h1>notFound</h1>;
};

export default notFound;
